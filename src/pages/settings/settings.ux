<import name="title-bar" src="@components/TitleBar/TitleBar.ux"></import>
<import name="input-item" src="@components/SettingItem/InputItem.ux"></import>

<template>
  <div class="page">
    <scroll scroll-y="true" class="scroll {{scrclass}}" bounces="true">
      <title-bar title="设置"></title-bar>
      <input-item multiLine="{{false}}" title="服务器URL" value="{{serverURL}}" fn="{{'server'}}"></input-item>
    </scroll>
  </div>
</template>

<script>
import router from "@system.router"

export default {
  private: {
    scrclass: "",
    serverURL: "",
    test: ["1207826398", "2701652670"]
  },
  onInit() {
    this.serverURL = global.config.read(c => c.napcat_httpserver_url);
  },
  goCT() {
    router.push({
      uri: "pages/test/comptest"
    })
  }
}
</script>

<style lang="less">
@import '@less/global.less';
</style>
